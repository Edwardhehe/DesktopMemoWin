<Window x:Class="DesktopMemo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DesktopMemo.Converters"
        x:Name="MemoWindow"
        Title="桌面备忘录"
        FontFamily="Microsoft YaHei UI"
        Height="820"
        Width="780"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Top="0">

    <Window.Resources>
        <LinearGradientBrush x:Key="WindowGlowBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F4EEF2" Offset="0"/>
            <GradientStop Color="#EEE7EC" Offset="0.56"/>
            <GradientStop Color="#E6DAD9" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="PanelBrush" Color="#CFF4EFF3"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#9F8FBEF5"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#39A66A"/>
        <DropShadowEffect x:Key="DropShadowEffect" Color="#35557C" Direction="270" ShadowDepth="12" BlurRadius="28" Opacity="0.14"/>

        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>
        <local:TodayForegroundConverter x:Key="TodayForegroundConverter"/>
        <local:BoolToFontWeightConverter x:Key="BoolToFontWeightConverter"/>
        <local:TextTruncateConverter x:Key="TextTruncateConverter"/>
        <local:BoolToTextDecorationConverter x:Key="BoolToTextDecorationConverter"/>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#BFF8FAFD"/>
            <Setter Property="BorderBrush" Value="#D5D9E2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#23415F"/>
        </Style>

        <Style x:Key="CalendarCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="#D6CBD2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Margin" Value="1.5"/>
            <Setter Property="MinHeight" Value="108"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <Style x:Key="ListCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="#D4CAD2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Border x:Name="MainWindowSurface" Background="{StaticResource WindowGlowBrush}" CornerRadius="28" BorderBrush="#D8CFD6" BorderThickness="1.2" Opacity="0.98" Effect="{StaticResource DropShadowEffect}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="27,27,0,0" Height="40" Background="#DDF3EEF5" BorderBrush="#CFC6CF" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <WrapPanel Margin="10,0" VerticalAlignment="Center">
                        <TextBlock Text="桌面备忘录" FontWeight="Bold" FontSize="13" Foreground="#214162"/>
                        <TextBlock Text="{Binding CurrentMonth, StringFormat='  {0:yyyy年MM月}'}" FontSize="10.5" Foreground="#70839A" VerticalAlignment="Bottom" Margin="0,0,10,0"/>
                        <TextBlock Text="视图" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#425B75" Margin="0,0,4,0" FontSize="10.5"/>
                        <ComboBox Width="84" Height="22" Margin="0,0,8,0" ItemsSource="{Binding ViewModes}" SelectedItem="{Binding SelectedViewMode}" VerticalContentAlignment="Center" FontSize="10.5"/>
                        <TextBlock Text="筛选" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#425B75" Margin="0,0,4,0" FontSize="10.5"/>
                        <ComboBox Width="84" Height="22" Margin="0,0,8,0" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" VerticalContentAlignment="Center" FontSize="10.5"/>
                        <Button Content="清空回收站" Command="{Binding EmptyRecycleBinCommand}" Style="{StaticResource ModernButtonStyle}" Width="78" Height="22" Margin="0,0,8,0" Visibility="{Binding IsRecycleBinView, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </WrapPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="◀" Command="{Binding PreviousMonthCommand}" Style="{StaticResource ModernButtonStyle}" Width="28" Height="22" Margin="1.5"/>
                        <Button Content="▶" Command="{Binding NextMonthCommand}" Style="{StaticResource ModernButtonStyle}" Width="28" Height="22" Margin="1.5"/>
                        <Button Content="⚙" Click="SettingsButton_Click" Style="{StaticResource ModernButtonStyle}" Width="30" Height="22" Margin="1.5"/>
                        <Button Content="×" Click="CloseButton_Click" Style="{StaticResource ModernButtonStyle}" Width="28" Height="22" Margin="1.5"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="6,2,6,1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Height="0" Width="0" Padding="0" Visibility="Collapsed" IsHitTestVisible="False"/>

                <Border Grid.Row="1" Background="Transparent" Padding="3,1" Margin="1,0,1,2" Visibility="{Binding IsMonthView, Converter={StaticResource BoolToVisibilityConverter}}">
                    <UniformGrid Columns="7">
                        <TextBlock Text="日" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#7D4C4C" FontSize="11"/>
                        <TextBlock Text="一" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#58708A" FontSize="11"/>
                        <TextBlock Text="二" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#58708A" FontSize="11"/>
                        <TextBlock Text="三" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#58708A" FontSize="11"/>
                        <TextBlock Text="四" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#58708A" FontSize="11"/>
                        <TextBlock Text="五" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#58708A" FontSize="11"/>
                        <TextBlock Text="六" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#3F73B0" FontSize="11"/>
                    </UniformGrid>
                </Border>

                    <ItemsControl Grid.Row="2" ItemsSource="{Binding CalendarDays}" Visibility="{Binding IsMonthView, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><UniformGrid Columns="7"/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CalendarCardStyle}" Opacity="{Binding IsCurrentMonth, Converter={StaticResource BoolToOpacityConverter}}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0" Margin="0,0,0,1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Command="{Binding DataContext.AddMemoCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Width="18" Height="18" Margin="0,0,4,0" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" FontFamily="Microsoft YaHei UI" FontWeight="Bold" FontSize="12" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Visibility="{Binding IsCurrentMonth, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="+" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                                            </Button>
                                            <TextBlock Grid.Column="2" Text="{Binding Date.Day}" HorizontalAlignment="Right" FontSize="11.5" Margin="0,0,4,0" FontWeight="{Binding IsToday, Converter={StaticResource BoolToFontWeightConverter}}" Foreground="{Binding IsToday, Converter={StaticResource TodayForegroundConverter}}" MouseLeftButtonDown="DateRow_MouseLeftButtonDown" Tag="{Binding}" Cursor="Hand"/>
                                        </Grid>
                                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Top" MaxHeight="84" Margin="0,1,0,0">
                                            <ItemsControl ItemsSource="{Binding Memos}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Tag="{Binding}" BorderThickness="1" CornerRadius="10" Margin="0.5,0.5" Padding="5,4">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
                                                                    <Setter Property="BorderBrush" Value="#9EDBB7"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                            <Setter Property="Background" Value="#DCEEE4"/>
                                                                            <Setter Property="BorderBrush" Value="#8DC7A7"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <Border.InputBindings>
                                                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DataContext.ShowMemoDetailCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" CommandParameter="{Binding}"/>
                                                            </Border.InputBindings>
                                                            <Border.ContextMenu>
                                                                <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                                                    <MenuItem Header="删除到回收站" Command="{Binding DataContext.DeleteMemoCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}"/>
                                                                    <MenuItem Header="标记为已完成" Command="{Binding DataContext.MarkCompletedCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding IsCompleted, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                                    <MenuItem Header="标记为未完成" Command="{Binding DataContext.MarkUncompletedCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                                    <Separator/>
                                                                    <MenuItem Header="置顶/取消置顶" Command="{Binding DataContext.TogglePinCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}"/>
                                                                    <MenuItem Header="优先级低" Command="{Binding DataContext.SetLowPriorityCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}"/>
                                                                    <MenuItem Header="优先级中" Command="{Binding DataContext.SetMediumPriorityCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}"/>
                                                                    <MenuItem Header="优先级高" Command="{Binding DataContext.SetHighPriorityCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}"/>
                                                                </ContextMenu>
                                                            </Border.ContextMenu>
                                                            <DockPanel>
                                                                <TextBlock Text="★" FontSize="8.5" Foreground="#C97D2D" Margin="0,0,3,0" Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}" DockPanel.Dock="Left"/>
                                                                <TextBlock Text="{Binding PriorityBadge}" FontSize="8" Foreground="#B45A3C" Margin="3,0,0,0" DockPanel.Dock="Right"/>
                                                                <TextBlock Text="{Binding Content, Converter={StaticResource TextTruncateConverter}, ConverterParameter=8}" FontSize="9.5" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip="{Binding Content}" TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToTextDecorationConverter}}">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Foreground" Value="#2D3748"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                    <Setter Property="Foreground" Value="#5E7E6E"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </DockPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                <Grid Grid.Row="2" Visibility="{Binding IsListView, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#D7F3EEF2" BorderBrush="#D7CDD4" BorderThickness="1" CornerRadius="16" Padding="12" Margin="4,0,4,10">
                            <StackPanel>
                                <TextBlock Text="{Binding ListSummary}" FontWeight="SemiBold" Foreground="#36516D"/>
                                <TextBlock Text="{Binding ListDescription}" Margin="0,5,0,0" FontSize="11" Foreground="#71849A" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="1" Background="#D7F3EEF2" BorderBrush="#D7CDD4" BorderThickness="1" CornerRadius="18" Padding="22" Margin="0,0,0,10" Visibility="{Binding IsFilteredMemosEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding EmptyStateTitle}" FontSize="15" FontWeight="SemiBold" Foreground="#2D4968" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding EmptyStateMessage}" Margin="0,8,0,0" FontSize="11" Foreground="#73869C" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="360"/>
                            </StackPanel>
                        </Border>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Visibility="{Binding HasFilteredMemos, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ItemsControl ItemsSource="{Binding FilteredMemos}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Tag="{Binding}">
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource ListCardStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                            <Setter Property="Background" Value="#DCEEE4"/>
                                                            <Setter Property="BorderBrush" Value="#8DC7A7"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Border.InputBindings>
                                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DataContext.ShowMemoDetailCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" CommandParameter="{Binding}"/>
                                            </Border.InputBindings>
                                            <Border.ContextMenu>
                                                <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                                    <MenuItem Header="恢复到正常列表" Command="{Binding DataContext.RestoreMemoCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="彻底删除（不可恢复）" Command="{Binding DataContext.PermanentlyDeleteMemoCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="删除到回收站" Command="{Binding DataContext.DeleteMemoCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="标记为已完成" Command="{Binding DataContext.MarkCompletedCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding IsCompleted, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="标记为未完成" Command="{Binding DataContext.MarkUncompletedCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <Separator/>
                                                    <MenuItem Header="置顶/取消置顶" Command="{Binding DataContext.TogglePinCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="优先级低" Command="{Binding DataContext.SetLowPriorityCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="优先级中" Command="{Binding DataContext.SetMediumPriorityCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                    <MenuItem Header="优先级高" Command="{Binding DataContext.SetHighPriorityCommand, Source={x:Reference MemoWindow}}" CommandParameter="{Binding}" Visibility="{Binding DataContext.IsRecycleBinView, Source={x:Reference MemoWindow}, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                                </ContextMenu>
                                            </Border.ContextMenu>
                                            <StackPanel>
                                                <WrapPanel>
                                                    <TextBlock Text="{Binding Date, StringFormat='{}{0:MM月dd日}'}" FontSize="11" Foreground="#6A7D92" Margin="0,0,10,0"/>
                                                    <Border Background="#F8E7D7" CornerRadius="9" Padding="6,2" Margin="0,0,8,0" Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="置顶" FontSize="10.5" Foreground="#9A5B2E"/>
                                                    </Border>
                                                    <Border Background="#FDE9D9" CornerRadius="9" Padding="6,2" Margin="0,0,8,0">
                                                        <TextBlock Text="{Binding PriorityText, StringFormat=优先级：{0}}" FontSize="10.5" Foreground="#B45A3C"/>
                                                    </Border>
                                                    <Border Background="#E6F6EA" CornerRadius="9" Padding="6,2" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="已完成" FontSize="10.5" Foreground="#3B7E5B"/>
                                                    </Border>
                                                </WrapPanel>
                                                <TextBlock Text="{Binding Content}" Margin="0,10,0,0" TextWrapping="Wrap" FontSize="12" LineHeight="20" TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToTextDecorationConverter}}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#24415F"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                    <Setter Property="Foreground" Value="#5E7E6E"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="{Binding CreatedAt, StringFormat='创建于 {0:yyyy-MM-dd HH:mm}'}" Margin="0,10,0,0" FontSize="10.5" Foreground="#8A98A6"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                </Grid>
            </Grid>

            <Border Grid.Row="2" CornerRadius="0,0,27,27" Height="24" Background="#D9F2ECEF" BorderBrush="#D5CCD4" BorderThickness="1,1,1,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                        <Ellipse Width="6" Height="6" Fill="{StaticResource SuccessBrush}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding ListSummary}" VerticalAlignment="Center" FontSize="9.5" Foreground="#36516D"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" Content="开机启动" IsChecked="{Binding IsStartupEnabled}" VerticalAlignment="Center" FontSize="9.5" Foreground="#36516D" Margin="0,0,8,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>


